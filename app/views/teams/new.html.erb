
<%= form_for @team do |f|%>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<br>

	<%= f.submit 'Save' %>
	<%= link_to 'Cancel', teams_path %>
<% end %>